---
globs: src/app/api/**/*.ts,src/server/**/*.ts
description: API 路由和服务器端开发规范
---

# API 路由和服务器端规范

## API 路由结构

### 路径组织

- **基础路径**：`src/app/api/v1/`
- **版本控制**：使用 `v1` 前缀进行 API 版本管理
- **RESTful 设计**：遵循 REST 约定

### 现有 API 端点

- `api/v1/accounts/` - 账户管理
- `api/v1/entries/` - 交易记录
- `api/v1/income/` - 收入管理
- `api/v1/reports/` - 报表数据
- `api/v1/rules/` - 规则配置
- `api/v1/valuations/` - 估值管理

## 路由文件规范

### 文件结构

```typescript
// src/app/api/v1/resource/route.ts
import { NextRequest, NextResponse } from "next/server";
import { auth } from "@/server/auth";

export async function GET(request: NextRequest) {
  // 身份验证
  const session = await auth();
  if (!session) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  try {
    // 业务逻辑
    const result = await serviceFunction();
    return NextResponse.json(result);
  } catch (error) {
    return NextResponse.json(
      { error: "Internal Server Error" },
      { status: 500 }
    );
  }
}
```

### HTTP 方法约定

- `GET` - 查询数据
- `POST` - 创建资源
- `PUT` - 更新资源（完整更新）
- `PATCH` - 部分更新
- `DELETE` - 删除资源

## 服务层架构

### 服务文件组织

- **位置**：`src/server/services/`
- **职责**：业务逻辑处理，不包含 HTTP 相关代码
- **现有服务**：
  - `audit.ts` - 审计日志
  - `fx.ts` - 汇率服务
  - `income.ts` - 收入计算
  - `ledger.ts` - 账本管理
  - `report.ts` - 报表生成
  - `rule.ts` - 规则引擎
  - `tax.ts` - 税收计算

### 服务层设计原则

```typescript
// 服务函数示例
export async function createAccount(data: CreateAccountInput) {
  // 1. 数据验证
  const validatedData = accountSchema.parse(data);

  // 2. 业务逻辑处理
  const account = await db.account.create({
    data: validatedData,
  });

  // 3. 审计日志
  await auditService.log("account.created", account.id);

  return account;
}
```

## 数据库访问

### Prisma 客户端使用

- **单例模式**：使用 [src/server/db.ts](mdc:src/server/db.ts) 中的 `db` 实例
- **事务处理**：对于复杂操作使用 Prisma 事务
- **错误处理**：统一处理数据库异常

### 查询优化

- 使用 `select` 指定需要的字段
- 合理使用 `include` 和 `select` 避免 N+1 查询
- 为复杂查询添加数据库索引

## 身份验证和授权

### 认证流程

- 使用 NextAuth.js（配置见 [src/server/auth.ts](mdc:src/server/auth.ts)）
- 每个受保护的 API 路由必须验证 session
- 统一的认证错误处理

### 权限控制

```typescript
// 权限检查示例
const session = await auth();
if (!session?.user?.id) {
  return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
}

// 资源所有权检查
const resource = await db.resource.findFirst({
  where: { id: resourceId, userId: session.user.id },
});
if (!resource) {
  return NextResponse.json({ error: "Forbidden" }, { status: 403 });
}
```

## 错误处理规范

### 统一错误响应格式

```typescript
// 成功响应
{ data: result }

// 错误响应
{ error: "错误描述", code?: "ERROR_CODE" }
```

### 错误状态码

- `400` - 请求参数错误
- `401` - 未认证
- `403` - 无权限
- `404` - 资源不存在
- `409` - 资源冲突
- `500` - 服务器内部错误

## 性能和安全

### 性能优化

- 使用适当的 HTTP 缓存头
- 对大数据集实现分页
- 避免在路由层进行复杂计算

### 安全措施

- 输入验证使用 Zod schema
- 防止 SQL 注入（使用 Prisma ORM）
- 敏感数据不记录到日志
- 使用环境变量管理敏感配置
