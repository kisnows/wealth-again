---
alwaysApply: true
description: 开发流程、构建和部署规范
---

# 开发流程与工作流

## 开发环境设置

### 启动开发服务器

```bash
# 使用 Turbopack 启动开发服务器（端口 4000）
npm run dev
```

### 构建和生产

```bash
# 生产构建
npm run build

# 运行已构建的应用
npm start
```

## 代码质量检查

### Linting 和格式化

基于 [biome.json](mdc:biome.json) 配置：

```bash
# 代码检查
npm run lint

# 自动格式化（写回文件）
npm run format
```

### 提交前检查清单

- [ ] 代码通过 lint 检查：`npm run lint`
- [ ] 代码已格式化：`npm run format`
- [ ] 所有测试通过：`npm test`
- [ ] 本地可以正常运行：`npm run dev`

## Git 工作流

### 提交规范

遵循 Conventional Commits 格式：

```
<type>(<scope>): <description>

<body>

<footer>
```

### 提交类型

- `feat:` - 新功能
- `fix:` - 错误修复
- `docs:` - 文档更新
- `style:` - 代码格式化（不影响功能）
- `refactor:` - 代码重构
- `test:` - 测试相关
- `chore:` - 构建工具、依赖更新

### 提交示例

```
feat(income): 添加薪资变动记录功能

- 新增薪资变动表单组件
- 实现薪资历史查询接口
- 添加相关测试用例

Closes #123
```

## 分支策略

### 主要分支

- `main` - 生产环境代码
- `develop` - 开发环境集成分支
- `feature/*` - 功能开发分支
- `hotfix/*` - 紧急修复分支

### 功能开发流程

1. 从 `develop` 创建 `feature/feature-name` 分支
2. 完成开发并通过所有检查
3. 创建 Pull Request 到 `develop`
4. 代码审查通过后合并

## Pull Request 规范

### PR 标题和描述

- 使用清晰的标题描述变更
- 在描述中关联相关 Issue：`Closes #123`
- 涉及 UI 变更需附截图
- 数据库变更需说明迁移步骤

### PR 检查清单

- [ ] 代码已通过本地测试
- [ ] 添加了必要的测试用例
- [ ] 更新了相关文档
- [ ] 数据库迁移已验证
- [ ] 无 linting 错误

## 环境配置

### 环境变量管理

使用 `.env` 文件管理环境变量：

```bash
# 数据库连接
DATABASE_URL="file:./prisma/dev.db"

# 认证配置
NEXTAUTH_SECRET="your-secret-key"
NEXTAUTH_URL="http://localhost:4000"
```

### 安全注意事项

- **禁止提交** `.env` 文件到版本库
- 敏感配置使用环境变量
- 服务端逻辑仅放在 `src/server`
- 避免在日志中输出敏感信息

## 依赖管理

### 包管理器

- 项目使用 `npm` 作为主要包管理器
- 支持 `pnpm` 用于 shadcn/ui 组件安装

### 依赖更新

```bash
# 检查过期依赖
npm outdated

# 更新依赖
npm update

# 安全审计
npm audit
```

## 部署流程

### 生产部署前检查

1. **数据库迁移**：确认 Prisma 迁移在生产环境可用
2. **环境变量**：配置生产环境的环境变量
3. **构建测试**：在类生产环境测试构建结果
4. **性能测试**：检查关键功能性能指标

### 部署命令

```bash
# 生产环境数据库迁移
npx prisma migrate deploy

# 生成 Prisma 客户端
npx prisma generate

# 构建应用
npm run build

# 启动生产服务器
npm start
```

## 故障排除

### 常见问题

1. **Prisma 客户端过期**：运行 `npx prisma generate`
2. **数据库连接失败**：检查 `DATABASE_URL` 环境变量
3. **端口冲突**：检查端口 4000 是否被占用
4. **依赖冲突**：删除 `node_modules` 重新安装

### 日志和调试

- 使用 `console.log` 进行开发调试
- 生产环境避免敏感信息日志
- 使用 Next.js 内置的错误边界处理错误
