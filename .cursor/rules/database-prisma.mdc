---
globs: prisma/**/*,src/server/db.ts
description: 数据库和 Prisma ORM 使用规范
---

# 数据库和 Prisma 规范

## Prisma 配置

### 核心文件

- **Schema 定义**：[prisma/schema.prisma](mdc:prisma/schema.prisma) - 数据模型定义
- **数据库连接**：[src/server/db.ts](mdc:src/server/db.ts) - Prisma 客户端单例
- **种子数据**：[prisma/seed.js](mdc:prisma/seed.js) - 初始数据填充

### 开发环境

- **数据库**：SQLite `prisma/prisma/dev.db`
- **迁移目录**：`prisma/migrations/`
- **生产环境**：通过 `DATABASE_URL` 环境变量配置

## 数据库操作命令

### 开发流程

```bash
# 应用迁移（开发环境）
npx prisma migrate dev

# 生成 Prisma 客户端
npx prisma generate

# 重置数据库并运行种子
npx prisma migrate reset

# 查看数据库内容
npx prisma studio
```

### 生产部署

```bash
# 生产环境迁移
npx prisma migrate deploy

# 生成客户端
npx prisma generate
```

## Schema 设计规范

### 模型命名

- 使用 PascalCase：`User`、`Account`、`Transaction`
- 关系字段使用 camelCase：`userId`、`accountId`
- 枚举使用 UPPER_SNAKE_CASE：`TRANSACTION_TYPE`

### 字段约定

```prisma
model Account {
  id        String   @id @default(cuid())
  name      String
  balance   Decimal  @db.Decimal(15, 2)
  currency  String   @default("CNY")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // 关系字段
  userId      String
  user        User         @relation(fields: [userId], references: [id])
  transactions Transaction[]

  @@map("accounts")
}
```

### 数据类型选择

- **金额**：`Decimal` 类型，精度 `@db.Decimal(15, 2)`
- **时间戳**：`DateTime`，自动管理 `createdAt`/`updatedAt`
- **ID**：使用 `cuid()` 生成唯一标识符
- **枚举**：定义明确的业务状态

## 数据库访问模式

### Prisma 客户端使用

```typescript
import { db } from "@/server/db";

// ✅ 推荐：使用统一的 db 实例
const accounts = await db.account.findMany({
  where: { userId: session.user.id },
  include: { transactions: true },
});

// ❌ 避免：直接实例化 PrismaClient
const prisma = new PrismaClient();
```

### 查询优化

```typescript
// ✅ 选择特定字段
const accounts = await db.account.findMany({
  select: {
    id: true,
    name: true,
    balance: true
  },
  where: { userId }
})

// ✅ 使用事务处理复杂操作
await db.$transaction(async (tx) => {
  await tx.account.update({ ... })
  await tx.transaction.create({ ... })
})
```

## 迁移管理

### 迁移文件规范

- **命名格式**：`YYYYMMDD_HHMMSS_description`
- **描述性名称**：如 `add_fixed_medical_personal`
- **向后兼容**：避免破坏性变更

### 迁移最佳实践

1. **小步迁移**：每次迁移只做一个逻辑变更
2. **数据备份**：重要变更前备份数据
3. **测试验证**：在开发环境充分测试
4. **回滚计划**：准备回滚策略

### 迁移示例

```sql
-- 添加新字段（安全）
ALTER TABLE "accounts" ADD COLUMN "description" TEXT;

-- 删除字段（需谨慎）
ALTER TABLE "accounts" DROP COLUMN "old_field";
```

## 种子数据管理

### 种子脚本规范

基于 [prisma/seed.js](mdc:prisma/seed.js)：

- 使用幂等操作（`upsert` 而非 `create`）
- 提供基础的测试数据
- 支持重复运行而不出错

### 种子数据示例

```javascript
// 幂等的用户创建
const user = await prisma.user.upsert({
  where: { email: "test@example.com" },
  update: {},
  create: {
    email: "test@example.com",
    name: "测试用户",
  },
});
```

## 性能和安全

### 查询性能

- 为经常查询的字段添加索引：`@@index([userId, createdAt])`
- 避免 N+1 查询，合理使用 `include`
- 对大数据集实现分页查询

### 数据安全

- 敏感数据加密存储
- 使用参数化查询防止 SQL 注入
- 定期备份数据库
- 环境变量管理数据库连接字符串

### 连接池管理

```typescript
// 生产环境连接池配置
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL") // 用于迁移
}
```
