---
globs: src/components/**/*.tsx,src/app/**/*.tsx
description: UI 组件开发规范和 shadcn/ui 使用约束
---

# UI 组件与样式约束

## 组件库限制

### shadcn/ui 强制使用

- **必须使用** shadcn/ui 作为唯一组件库
- **禁止引入** 其他 UI 组件库（如 Material-UI、Ant Design 等）
- shadcn 组件**仅放置**在 `src/components/ui/` 目录

### 新增组件流程

```bash
# 添加新的 shadcn 组件
pnpm dlx shadcn@latest add <component>
```

### 现有 shadcn 组件

基于 [components.json](mdc:components.json) 配置的可用组件：

- `@/components/ui/button`
- `@/components/ui/card`
- `@/components/ui/dialog`
- `@/components/ui/form`
- `@/components/ui/input`
- `@/components/ui/select`
- `@/components/ui/table`
- `@/components/ui/tabs`

## 样式系统约束

### Tailwind CSS 强制使用

- **必须使用** Tailwind CSS 作为唯一样式方案
- **禁止使用** CSS Modules、styled-components、emotion 等
- **禁止创建** 自定义 CSS 文件（除 `src/app/globals.css`）

### 样式编写规范

```tsx
// ✅ 推荐：使用 Tailwind 类名
<div className="flex items-center justify-between p-4 bg-white rounded-lg shadow-md">
  <Button variant="outline" size="sm">
    操作按钮
  </Button>
</div>

// ❌ 禁止：内联样式或自定义 CSS
<div style={{ display: 'flex' }}>
  <button className="custom-button">按钮</button>
</div>
```

## 组件开发规范

### 业务组件组织

- **位置**：`src/components/modules/`
- **命名**：PascalCase，如 `AccountTable.tsx`、`CreateAccountDialog.tsx`
- **职责**：业务逻辑组件，组合 shadcn/ui 基础组件

### 组件结构模板

```tsx
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";

interface ComponentProps {
  // 明确的 Props 类型定义
}

export function ComponentName({ ...props }: ComponentProps) {
  return (
    <Card className="p-6">
      {/* 组件内容 */}
      <Button variant="default">操作</Button>
    </Card>
  );
}
```

### 响应式设计

- 优先使用 Tailwind 响应式前缀：`sm:`、`md:`、`lg:`、`xl:`
- 移动优先设计原则
- 确保在不同屏幕尺寸下的可用性

## 主题和设计系统

### 颜色方案

基于 [components.json](mdc:components.json) 的 `neutral` 基色：

- 支持深色/浅色主题切换
- 使用 CSS 变量进行主题管理
- 遵循 shadcn/ui 的颜色语义

### 图标使用

- **图标库**：Lucide React（配置在 [components.json](mdc:components.json)）
- **导入方式**：`import { IconName } from "lucide-react"`
- **禁止使用**其他图标库

## 可访问性要求

- 为交互元素添加适当的 `aria-label`
- 确保键盘导航可用
- 遵循 WCAG 2.1 AA 标准
- 使用语义化 HTML 元素
