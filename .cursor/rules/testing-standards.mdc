---
globs: *.test.ts,*.test.tsx,*.spec.ts,*.spec.tsx
description: 测试规范和最佳实践
---

# 测试规范

## 测试框架和工具

- **测试框架**：Vitest（配置见 [vitest.config.ts](mdc:vitest.config.ts)）
- **测试设置**：[vitest.setup.ts](mdc:vitest.setup.ts)
- **覆盖率工具**：@vitest/coverage-v8

## 测试文件组织

### 位置和命名

- **位置**：统一放在 `src/tests/` 目录
- **命名**：文件以 `*.test.ts` 结尾
- **示例**：`income.test.ts`、`ledger.routes.test.ts`

### 测试分类

- **单元测试**：测试纯函数和工具模块（`src/lib`、`src/server`）
- **集成测试**：测试 API 路由和服务层交互
- **组件测试**：测试 React 组件行为

## 测试覆盖重点

### 优先级

1. **核心业务逻辑**：`src/server/services/` 中的服务
2. **API 路由**：`src/app/api/` 中的路由处理
3. **工具函数**：`src/lib/` 中的纯函数
4. **数据处理**：领域模型和计算逻辑

### 必须测试的场景

- 正常流程（Happy Path）
- 边界条件和异常情况
- 错误处理和验证逻辑
- 数据转换和格式化

## 测试命令

```bash
# 运行所有测试
npm test

# 监视模式运行测试
npm run test:watch

# 生成覆盖率报告
npm run test:coverage
```

## 测试写作规范

### 测试结构

```typescript
describe("模块名称", () => {
  describe("功能描述", () => {
    it("应该在特定条件下返回期望结果", () => {
      // Arrange - 准备测试数据
      // Act - 执行被测试的操作
      // Assert - 验证结果
    });
  });
});
```

### 测试原则

- **单一职责**：每个测试用例只验证一个行为
- **独立性**：测试之间不应有依赖关系
- **可读性**：测试名称应清楚描述测试场景
- **确定性**：测试结果应该是可预测和可重复的

## 测试辅助工具

- **测试帮助函数**：[src/tests/helpers.ts](mdc:src/tests/helpers.ts)
- **模拟数据**：在测试文件中定义或使用工厂函数
- **数据库测试**：使用内存数据库或事务回滚

## 回归测试要求

- **缺陷修复**：每个 bug 修复必须包含对应的回归测试
- **新功能**：新增功能必须包含完整的测试覆盖
- **重构**：重构后必须保证所有现有测试通过
