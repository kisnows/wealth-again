---
alwaysApply: true
description: 项目结构与模块组织规则
---

# 项目结构与模块组织

## 核心目录结构

- `src/app`：Next.js App Router 路由与 UI（如 `accounts/`、`entries/`、`api/`）。组件与路由就近放置。
- `src/lib`：共享工具（`utils.ts`、`utils/*`），通过路径别名 `@/*` 引用。
- `src/server`：仅服务器端代码（如 Prisma 客户端单例 `db.ts`）。
- `src/tests`：单元测试（Vitest 风格），文件命名为 `*.test.ts`。
- `prisma/`：Prisma 模型与迁移；本地开发使用 SQLite `dev.db`。
- `public/`：静态资源；`doc/`：产品/技术文档。

## 路径别名规则

- 优先使用 `@/*` 路径别名，避免相对路径深层穿越
- `@/components` → `src/components`
- `@/lib` → `src/lib`
- `@/server` → `src/server`
- `@/app` → `src/app`

## 文件组织原则

- React 组件使用 PascalCase 命名
- 与路由段就近放在 `src/app`
- 工具模块在 `src/lib`，文件名使用 camelCase
- 服务器端逻辑统一放在 `src/server`

## 关键配置文件

- [package.json](mdc:package.json)：项目依赖和脚本配置
- [tsconfig.json](mdc:tsconfig.json)：TypeScript 配置，使用严格模式
- [biome.json](mdc:biome.json)：代码格式化和 lint 配置
- [components.json](mdc:components.json)：shadcn/ui 组件配置
- [prisma/schema.prisma](mdc:prisma/schema.prisma)：数据库模式定义
