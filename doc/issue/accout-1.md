/account 中的快捷操作应该包含：存、取、转账，现在只有转账、记录估值，需要补足所有快捷功能，按照现有的 /account 中的快捷操作的实现方式，也就是一个弹窗来进行操作，全局共用相同的弹窗组件。
/account 下面的账户卡片的信息展示密度太低了，应该修改为一行展示两个，然后自适应，如果屏幕宽度不够再改为一行展示一个账户。
对于 /dashboard 中的快捷操作不应该链接到 /entries 页面，我看里面的功能都是落后的无用的，应该直接引用 /account 中的快捷操作弹窗组件，保持一致性。 或者直接删除这里的快捷操作入口。
/settings 中也有冗余信息，有多个地方设置展示币种，应该统一到一个地方设置。总之你需要对当前的 /settings 页面进行梳理，删除冗余信息，重新组织页面架构，使其更加清晰易用。